# 🌟 ORION 프로젝트 - React 전환 작업계획

## 📋 프로젝트 개요

- **프로젝트명**: ORION (별자리 분석 & 관측 서비스)
- **전환 대상**: HTML/CSS/JavaScript → React (Vite) + Styled-Components
- **현재 상태**: 메인 페이지(index.html) 완성, CSS/JS 보조 파일 완성
- **목표**: 모듈화된 React 컴포넌트 기반 프로젝트로 변환

---

## 📁 프로젝트 구조

```
src/
├── pages/
│   ├── MainPage.jsx          # 메인 페이지 (hero + 기능 카드)
│   ├── LoginPage.jsx         # 로그인 페이지
│   └── SignupPage.jsx        # 회원가입 페이지
├── components/
│   ├── Header.jsx            # 헤더/네비게이션 바 (GNB)
│   ├── Footer.jsx            # 푸터 (아직 미구현, 필요시 추가)
│   ├── ConstellationViewer.jsx # 별자리 뷰어 카드
│   ├── FeatureCard.jsx       # 기능 카드 (3열 그리드)
│   └── styles/
│       ├── Header.styles.js
│       ├── Footer.styles.js
│       ├── ConstellationViewer.styles.js
│       └── FeatureCard.styles.js
├── styles/
│   ├── GlobalStyles.js       # 글로벌 스타일 (reset, 배경 효과)
│   └── theme.js              # 테마 색상 변수
├── App.jsx                   # 라우팅 설정 (React Router)
└── main.jsx                  # 엔트리 포인트
```

---

## 🎯 작업 단계

### **Phase 1: 프로젝트 초기화 & 기본 설정**

#### Task 1-1: Vite + React 프로젝트 생성
```bash
npm create vite@latest front -- --template react
cd front
npm install
```

#### Task 1-2: 의존성 설치
```bash
npm install react-router-dom styled-components lucide-react
npm install -D tailwindcss postcss autoprefixer  # Tailwind 유틸리티 참고용
```

#### Task 1-3: 글로벌 스타일 설정
- `src/styles/GlobalStyles.js` 생성
  - Pretendard 웹폰트 import
  - 기본 reset CSS
  - 배경 성운 글로우 효과 (::before, ::after)
  - 커스텀 스크롤바 스타일
  
- `src/styles/theme.js` 생성
  - 색상 팔레트 (slate, purple, cyan 등)
  - Typography 설정
  - 간격, 그림자 등 디자인 토큰

---

### **Phase 2: 공통 컴포넌트 개발**

#### Task 2-1: Header 컴포넌트
**파일**: `src/components/Header.jsx`
- 로고 + 브랜드명 (ORION)
- 네비게이션 메뉴 (메인, 별자리 찾기, 별자리 위치 등 7개)
- 로그인/회원가입 버튼
- 스티키 헤더 (top-0, z-50)
- 배경 흐림 효과 (backdrop-blur)
- React Router Link 사용 (`navigate` 함수 → `useNavigate()` Hook)

**Styled-Components**: `src/components/styles/Header.styles.js`

#### Task 2-2: Footer 컴포넌트
**파일**: `src/components/Footer.jsx`
- 현재 HTML에 푸터가 없으므로 향후 추가 예상
- 기본 구조만 준비 (skeleton)

---

### **Phase 3: 페이지 컴포넌트 개발**

#### Task 3-1: MainPage 컴포넌트
**파일**: `src/pages/MainPage.jsx`
- Hero 섹션 (텍스트 + 버튼)
  - "밤하늘을 올려다보는 순간..." 텍스트
  - "사진으로 별자리 찾기" / "내 위치에서 찾아보기" 버튼
  
- ConstellationViewer 컴포넌트 포함 (col-span-5 카드)
  
- FeatureCard 컴포넌트 그리드 (3열)
  - 01: 사진으로 별자리 찾기
  - 02: 내 위치에서 별자리 찾기
  - 03: 별자리 정보

#### Task 3-2: LoginPage 컴포넌트
**파일**: `src/pages/LoginPage.jsx`
- 이메일 입력
- 비밀번호 입력
- "로그인 유지" 체크박스
- 로그인 버튼
- "회원가입" / "아이디/비밀번호 찾기" 링크
- 소셜 로그인 (카카오, 구글)
- Form 상태 관리 (useState)

#### Task 3-3: SignupPage 컴포넌트
**파일**: `src/pages/SignupPage.jsx`
- 이름 입력
- 생년월일 입력 (YYYYMMDD)
- 이메일 입력 (아이디@도메인 분리)
- 비밀번호 입력
- 비밀번호 확인
- 휴대폰 번호 입력
- 약관 동의 체크박스
- 회원가입 버튼
- "로그인" 링크
- Form 상태 관리 (useState)

---

### **Phase 4: 재사용 가능한 하위 컴포넌트 개발**

#### Task 4-1: ConstellationViewer 컴포넌트
**파일**: `src/components/ConstellationViewer.jsx`
- SVG 별자리 일러스트 (오리온자리)
- "LIVE SKY · SEOUL 21:42" 텍스트
- 나침반 아이콘
- 별자리 위치 정보 (하단)
- 관측 상태 배지 ("관측 좋음")

**Props**:
- `title`: 별자리명 (기본값: "오리온자리")
- `location`: 위치 정보 (기본값: "남동쪽 32°")
- `condition`: 관측 상태 (기본값: "관측 좋음")

#### Task 4-2: FeatureCard 컴포넌트
**파일**: `src/components/FeatureCard.jsx`
- 카드 번호 (01, 02, 03)
- 제목 (h3)
- 설명 텍스트
- "→" 링크 텍스트

**Props**:
- `number`: 카드 번호 (string)
- `title`: 제목 (string)
- `description`: 설명 (string)
- `linkText`: 링크 텍스트 (string)

---

### **Phase 5: 스타일 구성**

#### Task 5-1: GlobalStyles 구성
- Pretendard 웹폰트 import
- HTML/Body reset
- 배경 그래디언트 (purple + cyan radial-gradient)
- 스크롤바 커스텀
- Selection 스타일

#### Task 5-2: 컴포넌트별 Styled-Components
각 컴포넌트마다 `styles/ComponentName.styles.js` 파일 생성:

- **Header.styles.js**
  - HeaderWrapper (sticky, z-50, border-b)
  - Container
  - Logo (flex, cursor-pointer)
  - Nav (flex gap)
  - NavButton
  - ButtonGroup
  - AuthButton (primary/secondary)

- **ConstellationViewer.styles.js**
  - CardWrapper (gradient, border, backdrop-blur)
  - Header (flex justify-between)
  - SVGContainer
  - Footer (border-t)

- **FeatureCard.styles.js**
  - CardWrapper (rounded, hover effect)
  - Number
  - Title (h3)
  - Description
  - Link

---

### **Phase 6: 라우팅 & App 구성**

#### Task 6-1: App.jsx 구성
- React Router 설정
- 라우트 정의
  - `/` → MainPage
  - `/login` → LoginPage
  - `/signup` → SignupPage
  
#### Task 6-2: 네비게이션 함수
- `navigate()` 함수 → `useNavigate()` Hook으로 변경
- 헤더의 모든 네비게이션 버튼에 라우팅 연결

#### Task 6-3: main.jsx 구성
- React 엔트리 포인트
- GlobalStyles 적용
- App 컴포넌트 렌더링

---

### **Phase 7: 폼 처리 & 상태 관리**

#### Task 7-1: LoginPage 폼 처리
```javascript
const [formData, setFormData] = useState({
  email: '',
  password: '',
  remember: false
});

const handleChange = (e) => { /* ... */ };
const handleSubmit = (e) => { /* ... */ };
```

#### Task 7-2: SignupPage 폼 처리
```javascript
const [formData, setFormData] = useState({
  name: '',
  birthDate: '',
  emailId: '',
  emailDomain: '',
  password: '',
  passwordConfirm: '',
  phone: '',
  agreeTos: false
});
```

#### Task 7-3: 폼 검증
- 필수 필드 체크
- 이메일 형식 검증
- 비밀번호 확인 일치 여부
- 핸드폰 번호 형식 (010-****-****)

---

### **Phase 8: 아이콘 & 애니메이션**

#### Task 8-1: Lucide 아이콘 마이그레이션
- `lucide-react` 패키지 사용
  - Sparkles (로고)
  - ArrowRight (버튼)
  - Compass (별자리 뷰어)
  - MessageCircle (카카오)
  - Chrome (구글)

#### Task 8-2: CSS 애니메이션
- 펄스 효과 (별 애니메이션)
- 호버 효과 (카드, 버튼)
- 전환 효과 (색상, 배경)
- 부드러운 스크롤 (`smooth`)

---

## 🔄 마이그레이션 체크리스트

### 파일 구조
- [ ] `src/` 디렉토리 구조 생성
- [ ] `pages/`, `components/`, `styles/` 디렉토리 생성

### 컴포넌트
- [ ] Header 컴포넌트
- [ ] ConstellationViewer 컴포넌트
- [ ] FeatureCard 컴포넌트
- [ ] MainPage 페이지
- [ ] LoginPage 페이지
- [ ] SignupPage 페이지
- [ ] Footer 컴포넌트 (skeleton)

### 스타일
- [ ] GlobalStyles.js
- [ ] theme.js
- [ ] 각 컴포넌트별 styled-components

### 라우팅
- [ ] React Router 설정
- [ ] App.jsx 라우팅
- [ ] 네비게이션 함수 마이그레이션

### 기능
- [ ] 폼 상태 관리 (Login)
- [ ] 폼 상태 관리 (Signup)
- [ ] 폼 검증
- [ ] 소셜 로그인 버튼 (UI)

### 테스트
- [ ] 개발 서버 실행 (`npm run dev`)
- [ ] 모든 라우트 네비게이션 테스트
- [ ] 반응형 디자인 확인
- [ ] 크로스 브라우저 테스트

---

## 📊 작업 우선순위

1. **높음**: Phase 1~3 (초기화, 공통 컴포넌트, 페이지)
2. **중간**: Phase 4~5 (하위 컴포넌트, 스타일)
3. **보통**: Phase 6~7 (라우팅, 폼 처리)
4. **낮음**: Phase 8 (최적화, 애니메이션)

---

## 🚀 추가 고려사항

1. **API 연동**: 현재 폼은 `handleAuthSubmit()` Alert만 사용 → 향후 백엔드 API 연동 필요
2. **상태관리**: 복잡도 증가 시 Redux/Zustand 도입 고려
3. **타입 스크립트**: 선택사항 (현재 JavaScript로 진행)
4. **테스트**: Jest + React Testing Library로 단위 테스트 추가 가능
5. **성능**: 코드 스플리팅 (React.lazy)을 통한 번들 최적화

---

## 📝 기술 스택

| 항목 | 선택 |
|------|------|
| 빌드 도구 | Vite |
| 프레임워크 | React 18 |
| 라우터 | React Router v6 |
| 스타일 | Styled-Components |
| 아이콘 | Lucide React |
| 폰트 | Pretendard |
| 언어 | JavaScript |

---

## ✅ 완료 기준

- 모든 페이지가 원본 HTML과 동일한 UI/UX 제공
- React 컴포넌트 기반 모듈화 완성
- Styled-Components로 모든 스타일 관리
- 라우팅이 정상 작동
- 반응형 디자인 유지
- 개발 서버에서 오류 없이 실행

---

**작성일**: 2026-08-31  
**상태**: Ready for Implementation
