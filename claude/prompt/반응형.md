# ORION 반응형 웹사이트 작업 계획

## 📋 프로젝트 개요
- **목표**: CSS 변경만으로 기존 React 컴포넌트를 모바일 중심의 반응형 웹사이트로 전환
- **범위**: front/src 폴더의 *.styles.js 파일만 수정
- **기준 해상도**: 모바일(0-768px), 태블릿(768-1024px), 데스크톱(1024px+)

---

## 🎨 전체 설계 원칙

### 1. 미디어 쿼리 표준화
```
- 모바일: max-width: 768px (기본 레이아웃)
- 태블릿: 768px < width < 1024px (중간 레이아웃)
- 데스크톱: min-width: 1024px (현재 레이아웃)
```

### 2. 글로벌 스타일 수정 대상
- **GlobalStyles.js**: 기본 폰트 크기, 라인 높이, 여백 조정
- **theme.js**: 반응형 스페이싱, 브레이크포인트 정의

### 3. 스타일 작성 패턴
```javascript
// 모바일 우선 접근 (Mobile-First Approach)
const ComponentStyle = styled.div`
  /* 모바일(기본) 스타일 */
  padding: 1rem;
  font-size: 0.875rem;
  
  /* 태블릿 이상 */
  @media (min-width: 768px) {
    padding: 1.5rem;
  }
  
  /* 데스크톱 이상 */
  @media (min-width: 1024px) {
    padding: 2rem;
  }
`
```

---

## 📱 페이지별 반응형 설계

### 1. Header (헤더) - 가장 우선순위 높음

#### 모바일 레이아웃 (< 768px)
- **상단 바 높이**: 4rem 유지
- **좌측**: ORION 로고만 표시
- **우측**: 햄버거 메뉴 버튼(☰) 추가
- **로그인/회원가입 버튼**: 햄버거 메뉴 내에 이동
- **내비게이션 숨김**: 기존 인라인 네비게이션 숨기기

#### 햄버거 메뉴 구조
```
☰ 버튼 클릭 시:
├─ 별자리 정보
├─ 도감
├─ 운세
├─ 마이 페이지
├─ 로그인
└─ 회원가입
```
- 메뉴 스타일: 슬라이드 방식 또는 모달 형태
- 배경: 어두운 반투명 (backdrop-filter 활용)
- 닫기: 메뉴 영역 외부 클릭 또는 X 버튼

#### 수정할 파일
- `src/components/styles/Header.styles.js`
- `src/components/Header.jsx` (햄버거 메뉴 로직 추가)

#### 구체적 CSS 변경사항
```javascript
// Header.styles.js
@media (max-width: 768px) {
  // 네비게이션 숨기기
  const Nav = styled.nav`
    display: none;
  `
  
  // 햄버거 버튼 표시
  const HamburgerButton = styled.button`
    display: block;
    background: none;
    border: none;
    color: white;
    font-size: 1.5rem;
    cursor: pointer;
  `
  
  // 모바일 메뉴
  const MobileMenu = styled.div`
    position: fixed;
    top: 0;
    right: 0;
    width: 80%;
    max-width: 300px;
    height: 100vh;
    background: rgba(15, 23, 42, 0.95);
    backdrop-filter: blur(12px);
    transform: translateX(${props => props.isOpen ? '0' : '100%'});
    transition: transform 300ms ease-in-out;
    z-index: 1000;
    padding: 1.5rem;
  `
}

// 데스크톱
@media (min-width: 1024px) {
  // 기존 스타일 유지
  display: flex;
  gap: 2rem;
}
```

---

### 2. 회원가입 페이지 (SignupPage)

#### 모바일 레이아웃 (< 768px)
- **전체 구조**: 세로 스크롤 가능한 단일 컬럼
- **입력 필드**: 모두 Full-width (100% - 패딩)
- **필드 높이**: 3.5rem (터치 편의성)
- **필드 간 간격**: 1.25rem
- **라벨**: 필드 위에 위치, 폰트 크기 0.875rem
- **버튼**: Full-width, 높이 3.5rem, 맨 하단 고정 또는 스크롤 끝

#### 인증 페이지 예외 처리
- **내비게이션 제거**: 햄버거 메뉴 버튼도 숨기기
- **로고만 표시**: 상단 좌측에 ORION 로고만
- **배경**: 깔끔한 다크 배경 유지
- **폼 중심화**: 시각적 집중도 극대화

#### 수정할 파일
- `src/pages/styles/SignupPage.styles.js`
- `src/pages/SignupPage.jsx`

#### 구체적 CSS 변경사항
```javascript
// SignupPage.styles.js
const SignupContainer = styled.div`
  /* 모바일 */
  padding: 1rem;
  
  @media (min-width: 768px) {
    padding: 2rem;
    max-width: 600px;
    margin: 0 auto;
  }
  
  @media (min-width: 1024px) {
    padding: 3rem;
    max-width: 800px;
  }
`

const FormField = styled.div`
  /* 모바일 */
  margin-bottom: 1.25rem;
  width: 100%;
  
  @media (min-width: 768px) {
    margin-bottom: 1.5rem;
  }
`

const Input = styled.input`
  /* 모바일 */
  width: 100%;
  padding: 1rem;
  height: 3.5rem;
  font-size: 1rem;
  border-radius: 0.5rem;
  
  @media (min-width: 768px) {
    font-size: 0.9375rem;
  }
`

const SubmitButton = styled.button`
  /* 모바일 */
  width: 100%;
  height: 3.5rem;
  margin-top: 1.5rem;
  margin-bottom: 2rem;
  font-size: 1rem;
  
  @media (min-width: 768px) {
    margin-top: 2rem;
  }
`
```

#### 추가 고려사항
- 비밀번호 필드 보기/숨기기 아이콘 추가
- 에러 메시지: 필드 아래에 작은 폰트(0.75rem)로 표시
- 선택적 필드 표시 (필수/선택)

---

### 3. 로그인 페이지 (LoginPage)

#### 모바일 레이아웃 (< 768px)
- **구조**: 회원가입과 동일 (Full-width 필드, 세로 배치)
- **특수 요소**:
  - "로그인 유지" 체크박스: 좌측 정렬, 작은 크기
  - 비밀번호 찾기 링크: 우측 정렬, 0.8125rem
- **버튼**: Full-width 로그인 버튼
- **회원가입 링크**: 로그인 버튼 아래, 중앙 정렬

#### 헤더 예외 처리
- **내비게이션 제거**: 로그인 페이지도 인증 페이지로 처리
- **최소한의 요소만**: 로고 + 콘텐츠

#### 수정할 파일
- `src/pages/styles/LoginPage.styles.js`

---

### 4. 회원정보 수정 페이지 (EditProfilePage)

#### 모바일 레이아웃 (< 768px)
- **구조**: 회원가입 페이지와 동일
- **추가 요소**:
  - 프로필 이미지 업로드 섹션 (상단)
  - 변경 전/후 비교 토글 (선택사항)
- **버튼**: "수정 완료" 또는 "저장" (Full-width)
- **추가 버튼**: 회원 탈퇴 버튼 (아래쪽, 아웃라인 스타일)

#### 수정할 파일
- `src/pages/styles/EditProfilePage.styles.js`

---

### 5. 별자리 찾기 페이지 (ConstellationFindPage)

#### 모바일 레이아웃 (< 768px)
- **레이아웃**: 세로 구조
- **이미지 업로드 영역**:
  - Full-width 드래그 앤 드롭 영역
  - 높이: 200px (모바일), 300px (태블릿)
  - 테두리: dashed, 1px
  - 배경: 어두운 색상 (contrast 확보)
  - 초기 텍스트: "사진 선택" 버튼

#### 버튼 상태 변경
- **초기 상태**: "사진 선택" 버튼
- **이미지 선택 후**: 
  - 선택된 이미지 미리보기 표시
  - "사진 선택" → "별자리 분석하기" 버튼으로 변경
  - 버튼 스타일: 활성화 (배경색, 커서 등)

#### 코드 구조
```javascript
// ConstellationFindPage.jsx
const [selectedImage, setSelectedImage] = useState(null);
const [isAnalyzing, setIsAnalyzing] = useState(false);

// 이미지 선택 시
const handleImageSelect = (file) => {
  setSelectedImage(file);
  // 미리보기 설정
};

// 버튼 텍스트 및 상태
const buttonText = selectedImage ? "별자리 분석하기" : "사진 선택";
const isButtonEnabled = !!selectedImage;

// ConstellationFindPage.styles.js
const UploadArea = styled.div`
  /* 모바일 */
  width: 100%;
  height: 200px;
  border: 2px dashed #9333ea;
  border-radius: 0.5rem;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  transition: all 200ms ease;
  
  &:hover {
    background-color: rgba(147, 51, 234, 0.05);
  }
  
  @media (min-width: 768px) {
    height: 300px;
  }
`

const AnalyzeButton = styled.button`
  /* 모바일 */
  width: 100%;
  height: 3.5rem;
  margin-top: 1.5rem;
  font-size: 1rem;
  
  /* 초기 상태 (disabled) */
  ${props => !props.disabled && `
    background-color: #9333ea;
    color: white;
    cursor: pointer;
    
    &:hover {
      background-color: #a855f7;
    }
  `}
  
  ${props => props.disabled && `
    background-color: #64748b;
    color: #cbd5e1;
    cursor: not-allowed;
  `}
`
```

#### 수정할 파일
- `src/pages/styles/ConstellationFindPage.styles.js`
- `src/pages/ConstellationFindPage.jsx`

---

### 6. 별자리 위치 찾기 페이지 (ConstellationLocationPage)

#### 모바일 레이아웃 (< 768px)
- **지도/뷰어 영역**: Full-width, 높이 300-400px
- **정보 패널**: 지도 아래 또는 슬라이드 시트로 표시
- **컨트롤 버튼들**: 하단에 row 형태로 배치 (Full-width 중분할)
- **스크롤**: 세로 스크롤 가능

#### 수정할 파일
- `src/pages/styles/ConstellationLocationPage.styles.js`

---

### 7. 메인 페이지 (MainPage)

#### 모바일 레이아웃 (< 768px)
- **히어로 섹션**:
  - 제목 폰트 크기: 1.5rem (모바일), 2.25rem (데스크톱)
  - 부제목: 1rem
  - 높이: 가변 (컨텐츠에 따라)
  - 별 배경: 유지

- **별자리 정보 카드**:
  - 그리드: 1열 (모바일), 2열 (태블릿), 여러 열 (데스크톱)
  - 카드 높이: 자동 조정
  - 카드 마진: 1rem (모바일), 1.5rem (태블릿)

- **기타 섹션**:
  - Full-width 배치
  - 여백 감소 (모바일)

#### 수정할 파일
- `src/pages/styles/MainPage.styles.js`

---

### 8. 기타 페이지

#### ConstellationInfoPage (별자리 정보 페이지)
- 제목: 1.5rem (모바일), 2rem+ (데스크톱)
- 설명 텍스트: 1rem (모바일), 1.0625rem (데스크톱)
- 이미지: Full-width
- 정보 박스: Full-width, 1열 (모바일)

#### ConstellationCatalogPage (도감 페이지)
- 그리드: 2열 (모바일), 3열 (태블릿), 4+ 열 (데스크톱)
- 카드 크기: 동일 유지
- 검색/필터: 모바일 친화적 (전체 너비)

#### FortuneReadingPage (운세 페이지)
- 입력 폼: 회원가입과 동일 스타일
- 결과 카드: Full-width

#### MyPage (마이 페이지)
- 프로필 섹션: 중앙 정렬, 이미지 크기 100px
- 메뉴: 1열 (모바일), 여러 열 (데스크톱)
- 버튼들: Full-width (모바일)

---

## 🛠️ 구현 순서 (우선순위)

### Phase 1: 기반 작업 (필수)
1. **GlobalStyles.js, theme.js** 수정
   - 브레이크포인트 상수 정의
   - 반응형 마진/패딩 유틸리티 추가
   - 모바일 폰트 크기 기준값 설정

2. **Header.styles.js & Header.jsx**
   - 햄버거 메뉴 로직 구현
   - 모바일/데스크톱 조건부 렌더링
   - 메뉴 애니메이션 구현

### Phase 2: 인증 페이지 (높은 우선순위)
3. **LoginPage.styles.js & LoginPage.jsx**
   - 전체 너비 필드
   - 헤더 예외 처리
   - 폼 스타일 적용

4. **SignupPage.styles.js & SignupPage.jsx**
   - 전체 너비 필드
   - 헤더 예외 처리
   - 다중 필드 레이아웃

### Phase 3: 사용자 페이지 (중간 우선순위)
5. **EditProfilePage.styles.js & EditProfilePage.jsx**
   - 회원가입과 동일 구조
   - 프로필 이미지 영역 추가

### Phase 4: 주요 기능 페이지 (중간 우선순위)
6. **ConstellationFindPage.styles.js & ConstellationFindPage.jsx**
   - 이미지 업로드 영역
   - 버튼 상태 변경 로직

7. **MainPage.styles.js**
   - 카드 그리드 반응형
   - 히어로 섹션 스타일

### Phase 5: 보조 페이지 (낮은 우선순위)
8. **ConstellationLocationPage.styles.js**
9. **ConstellationInfoPage.styles.js**
10. **ConstellationCatalogPage.styles.js**
11. **FortuneReadingPage.styles.js**
12. **MyPage.styles.js**

---

## 📐 반응형 규칙 체크리스트

- [ ] 모든 페이지의 입력 필드가 모바일에서 최소 3.5rem 높이 유지
- [ ] 터치 타겟: 최소 44x44px (모바일)
- [ ] 폰트 크기 모바일: 최소 0.875rem, 최대 1.25rem
- [ ] 패딩/마진 모바일: 1rem 이상 (화면 가장자리)
- [ ] 이미지: max-width 100% 적용
- [ ] 버튼: Full-width (모바일), 가변 너비 (데스크톱)
- [ ] 색상 대비: WCAG AA 표준 (4.5:1)
- [ ] 마우스 호버: 터치 기기에서 제거 또는 활성 상태로 변경
- [ ] 헤더/네비: 모바일에서 sticky 유지, 높이 조정
- [ ] 스크롤: 수평 스크롤 없음 (1024px 이상에서 확인)

---

## 🎯 테스트 계획

### 기기별 테스트
- iPhone SE (375px)
- iPhone 12/13 (390px)
- iPad (768px)
- MacBook (1440px+)

### 테스트 항목
1. 모든 페이지 레이아웃 확인
2. 폼 입력 및 제출 동작
3. 미디어 쿼리 동작 (개발자 도구 모바일 모드)
4. 터치 인터페이스 테스트 (해당 시 실제 기기)
5. 성능 확인 (페이지 로드 속도, 스크롤 부드러움)
6. 접근성 테스트 (화면 읽기, 키보드 네비게이션)

---

## 📝 추가 참고사항

### 색상 팔레트 (기존 유지)
- 주색: #9333ea (purple)
- 보조색: #a855f7 (lighter purple)
- 텍스트: #cbd5e1 (slate-300)
- 배경: #0f172a (slate-950)

### 폰트
- 기본: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto
- 한글 최적화 필요

### 애니메이션/트랜지션
- 모든 상태 변화: 150-300ms ease-in-out
- 햄버거 메뉴: 300ms

### 성능 최적화
- 불필요한 렌더링 최소화
- 이미지 최적화 (WebP, 지연 로딩)
- CSS 번들 크기 모니터링

---

## 📞 문의 및 피드백
- 디자인 관련 의사결정 필요 시: 모바일 UI 가이드 참고
- 기술적 이슈: 각 파일별 주석 추가하여 문서화
